use airdrop_rust::{
    base58_to_wallet, claim_airdrop, mint_tx, transfer_all_sol, transfer_sol, wallet_to_base58,
};
fn main() {
    // base58_to_wallet();
    // wallet_to_base58();
    // claim_airdrop();
    // transfer_sol();
    // transfer_all_sol();
    mint_tx();
}
